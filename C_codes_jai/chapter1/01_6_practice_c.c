#include <stdio.h>

int main()
{
    float celcius = 37, far = (celcius * 9 / 5) + 32; //you can add many float byjust putting comma
    printf("The value of this celcius temperature in farenheit is %f", far);

    return 0;
}