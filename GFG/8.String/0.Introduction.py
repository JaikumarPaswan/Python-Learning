#Gives order of position or Unicode code point for a given character.
print(ord("a"))
print(ord("A"))

#Gives character of that Unicode integer
print(chr(97))
print(chr(65))


# Strings are immutable
s = "geeks"

# s[0] = "e"
# print(s)


#Access string elements, we can also access it using index
a="apple"
for i in a:
    print(i)